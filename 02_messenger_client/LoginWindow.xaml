<Window x:Class="_02_messenger_client.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Enter nickname" Height="160" Width="360" WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Enter your nickname:" FontSize="14" Margin="2"/>
        <TextBox x:Name="txtNick" Grid.Row="1" Margin="0,8,0,0" Height="30"
                 VerticalContentAlignment="Center"
                 HorizontalAlignment="Stretch"
                 MinWidth="200"
                 TextChanged="TxtNick_TextChanged"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button x:Name="btnCancel" Content="Cancel" Width="80" Margin="0,0,8,0" Click="BtnCancel_Click"/>
            <Button x:Name="btnOk" Content="OK" Width="80" IsEnabled="False" Click="BtnOk_Click"/>
        </StackPanel>
    </Grid>
</Window>
