<Window x:Class="_04_Mail_Client.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_04_Mail_Client"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <!-- БЫЛО 4 строки, добавим 5-ю под кнопки -->
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="4*"/>
            <RowDefinition Height="Auto"/>

        </Grid.RowDefinitions>


        <Label Grid.Row="0" FontSize="20" FontWeight="Bold"
           HorizontalContentAlignment="Center" Padding="10">From :</Label>
        <Label Grid.Row="1" FontSize="20" FontWeight="Bold"
           HorizontalContentAlignment="Center" Padding="10">To :</Label>
        <Label Grid.Row="2" FontSize="20" FontWeight="Bold"
           HorizontalContentAlignment="Center" Padding="10">Theme :</Label>


        <TextBox x:Name="fromTextBox" Grid.Column="1" Margin="5" VerticalContentAlignment="Center"/>
        <TextBox x:Name="toTextBox" Grid.Row="1" Grid.Column="1" Margin="5"/>
        <TextBox x:Name="themeTextBox" Grid.Row="2" Grid.Column="1" Margin="5"/>


        <TextBox x:Name="bodyTextBox"
             Grid.Row="3" Grid.ColumnSpan="2" Margin="5"
             AcceptsReturn="True"
             VerticalScrollBarVisibility="Auto"
             TextWrapping="Wrap"/>


        <StackPanel Grid.Row="4" Grid.ColumnSpan="2"
            Orientation="Horizontal"
            HorizontalAlignment="Right"
            Margin="5" >
            <TextBlock x:Name="fileNameText"
               VerticalAlignment="Center"
               Margin="0,0,10,0"/>
            <Button x:Name="attachButton" Click="attachButton_Click" Margin="5" Padding="12,6">Attach</Button>
            <Button x:Name="sendButton" Click="sendButton_Click" Margin="5" Padding="12,6">Send</Button>
            <Button x:Name="exitButton" Click="exitButton_Click" Margin="5" Padding="12,6">Exit</Button>
        </StackPanel>

    </Grid>
</Window>
