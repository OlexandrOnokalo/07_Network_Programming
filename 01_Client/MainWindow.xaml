<Window x:Class="_01_Client.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Ptitsa govorun otlichaetsya umom i soobrazitelnostyu" Height="600" Width="600">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,8">
            <TextBlock VerticalAlignment="Center" Margin="0,0,8,0">IP:</TextBlock>
            <TextBox x:Name="txtIP" Width="120" Margin="0,0,12,0" />
            <TextBlock VerticalAlignment="Center" Margin="0,0,8,0">Port:</TextBlock>
            <TextBox x:Name="txtPort" Width="80" Margin="0,0,12,0" />
        </StackPanel>

        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,0,0,8">
            <TextBox x:Name="txtMessage" Width="420" Height="30" VerticalContentAlignment="Center" />
            <Button x:Name="btnSend" Width="80" Height="30" Margin="8,0,0,0">Send</Button>
        </StackPanel>

        <ListBox x:Name="lstDialog" Grid.Row="2">
            <ListBox.Background>
                <ImageBrush ImageSource="Img/bg.jpg"
                    Stretch="UniformToFill"
                    Opacity="0.4"/>

            </ListBox.Background>
        </ListBox>
    </Grid>
</Window>
